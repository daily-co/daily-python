Installation
=======================================

`daily-python` can be easily installed using `pip`:

.. code-block:: bash

   pip install daily-python

To upgrade:

.. code-block:: bash

   pip install -U daily-python
